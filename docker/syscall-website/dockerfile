FROM nginx:alpine

# 1. Remove default config that listens on port 80
RUN rm /etc/nginx/conf.d/default.conf

# 2. Copy custom config listening on port 8090
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 3. Copy website content
COPY . /usr/share/nginx/html

# 4. Expose port 8090
EXPOSE 8090
